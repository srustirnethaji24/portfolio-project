document.addEventListener("DOMContentLoaded", function () {
    //Render skill
    //renderSkills(skills);
    renderFilters(skills);
    initThemeToggle();
    typeEffect();
    initProjectSearch();
    //initModal();
    initContactValidation() ;
})

